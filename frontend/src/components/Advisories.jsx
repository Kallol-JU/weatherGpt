export function Advisories({ location, onAsk }) { const tabs=['🌾 Farming','✈️ Aviation','🌊 Marine','🏙️ Urban']; return <section className="page-section"><div className="section-title"><div><h1>Advisories</h1><p>Turn weather data into simple, useful decisions.</p></div></div><div className="advisory-tabs">{tabs.map((x,i)=><button className={i===0?'active':''} key={x}>{x}</button>)}</div><div className="advisory-card"><div className="advisory-good">✓ <div><strong>Weather guidance for {location.name}</strong><p>Ask WeatherGPT for a location-specific advisory based on live meteorological data.</p></div></div><div className="advisory-actions"><button onClick={()=>onAsk('Is today good for farming?')}>🌾 Farming advice</button><button onClick={()=>onAsk('What are the aviation conditions?')}>✈️ Aviation</button><button onClick={()=>onAsk('What are the marine conditions?')}>🌊 Marine</button><button onClick={()=>onAsk('Is there a weather risk for the city?')}>🏙️ Urban risk</button></div></div></section> }
