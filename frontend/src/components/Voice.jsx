import { useState } from 'react'
export function Voice({ onAsk, language }) { const [listening,setListening]=useState(false); return <section className="page-section"><div className="section-title"><div><h1>Voice WeatherGPT</h1><p>Ask questions hands-free in supported languages.</p></div></div><div className={`voice-card ${listening?'listening':''}`}><div className="voice-rings"><button onClick={()=>setListening(!listening)}>{listening?'■':'🎙️'}</button></div><h2>{listening?'Listening…':'Speak naturally'}</h2><p>{listening?'Try: “Will it rain tomorrow?”':'Tap the microphone and ask about the weather.'}</p><button className="primary-btn" onClick={()=>{setListening(!listening);if(!listening)onAsk('What is the weather today?')}}>{listening?'Stop listening':'Start speaking'}</button><small>Language: {language==='HI'?'Hindi':'English'} · Browser speech support varies by device.</small></div></section> }
